runtime: python311

entrypoint: gunicorn -b :$PORT main:app

instance_class: F2

env_variables:
  GOOGLE_CLOUD_PROJECT: pcloud2025-1-454210

handlers:
- url: /static
  static_dir: static

- url: /.*
  script: auto

